var blackWork = 300;
const valueSimulation = document.querySelectorAll('#valorSimulation');
const btnSimulator = document.querySelectorAll('.btn-whatsapp');

btnSimulator.addEventListener('click', (valueSimulation) => {
    valueSimulation.textContent = "300";
});